<view class="playlist-container" bindtap="goMusicPlay" data-id="id">
    <image class="img" src="{{item.picUrl}}"></image>
    <text class="play-count">{{_count}}</text>
    <text class="text-name">{{item.name}}</text>
</view>